var char = prompt("Enter any character").toLowerCase();

if (char === 'a' || char === 'e' || char === 'i' || char === 'o' || char === 'u') {
    document.write(`It is a vowel <br>`);
} else {
    document.write(`It is not a vowel <br>`);
}
